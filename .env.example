# CloudWisePod 环境变量（复制为 .env 并填写）
# 会话密钥：openssl rand -hex 32
SESSION_SECRET=
# Groq API key（主力）：https://console.groq.com/keys
GROQ_API_KEY=
# OpenAI API key（兜底，可选；留空则无法切换到 openai provider）
OPENAI_API_KEY=
# 站点根 URL
BASE_URL=http://localhost:8080
